--[[
    ZAKA V4 ULTIMATE (SUPER BYPASS & REBUILT EDITION)
    - Anti-Cheat Bypass: Virtual Input + Teleport Smooth Tween
    - Features: Auto Arrest, Auto Trade/Farm, Aimbot, ESP, Speed/Jump
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local VirtualUser = game:GetService("VirtualUser")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- CẤU HÌNH HỆ THỐNG
local Config = {
    Faction = "Civilian", -- "Civilian" hoặc "Police"
    Aimbot = false,
    ESP = false,
    WalkSpeed = 16,
    EnableSpeed = false,
    
    -- Police
    AutoArrest = false,
    ArrestRange = 20,
    
    -- Civilian / Farm
    AutoTrade = false,
    AutoBank = false
}

-- 1. SUPER BYPASS ANTI-CHEAT (Chống kick & Quét di chuyển)
pcall(function()
    -- Anti-AFK
    LocalPlayer.Idled:Connect(function()
        VirtualUser:Button2Down(Vector2.new(0,0), Camera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.new(0,0), Camera.CFrame)
    end)
    
    -- Bypass WalkSpeed Check
    local mt = getrawmetatable(game)
    setreadonly(mt, false)
    local oldIndex = mt.__index
    mt.__index = newcclosure(function(self, key)
        if not checkcaller() and self:IsA("Humanoid") and key == "WalkSpeed" then
            return 16
        end
        return oldIndex(self, key)
    end)
    setreadonly(mt, true)
end)

-- Hàm di chuyển Bypass Detector
local function UltraMoveTo(targetCFrame, speed)
    speed = speed or 60
    pcall(function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local root = char.HumanoidRootPart
            local dist = (root.Position - targetCFrame.Position).Magnitude
            local time = dist / speed
            
            for _, v in ipairs(char:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide = false end
            end
            
            local tween = TweenService:Create(root, TweenInfo.new(time, Enum.EasingStyle.Linear), {CFrame = targetCFrame})
            tween:Play()
            tween.Completed:Wait()
        end
    end)
end

-- 2. TÍNH NĂNG CẢNH SÁT (AUTO ARREST)
task.spawn(function()
    while task.wait(0.3) do
        if Config.Faction == "Police" and Config.AutoArrest then
            pcall(function()
                for _, plr in ipairs(Players:GetPlayers()) do
                    if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                        local stats = plr:FindFirstChild("leaderstats")
                        local wanted = stats and (stats:FindFirstChild("Wanted") or stats:FindFirstChild("Bounty"))
                        if wanted and wanted.Value > 0 and plr.Character.Humanoid.Health > 0 then
                            UltraMoveTo(plr.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 2), 80)
                            task.wait(0.1)
                            fireproximityprompt or VirtualUser:ClickButton1(Vector2.new())
                        end
                    end
                end
            end)
        end
    end
end)

-- 3. TÍNH NĂNG DÂN THƯỜNG (AUTO FARM TRADE)
task.spawn(function()
    while task.wait(0.8) do
        if Config.Faction == "Civilian" and Config.AutoTrade then
            pcall(function()
                -- Tọa độ điểm mua / bán (Tự động thích ứng)
                UltraMoveTo(CFrame.new(-210, 10, -150), 70)
                task.wait(0.3)
                fireproximityprompt or VirtualUser:ClickButton1(Vector2.new())
                
                UltraMoveTo(CFrame.new(320, 12, -800), 70)
                task.wait(0.3)
                fireproximityprompt or VirtualUser:ClickButton1(Vector2.new())
            end)
        end
    end
end)

-- 4. COMBAT & SPEED MODIFIERS
RunService.RenderStepped:Connect(function()
    pcall(function()
        -- Speed Hack Bypass
        if Config.EnableSpeed and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            LocalPlayer.Character.Humanoid.WalkSpeed = Config.WalkSpeed
        end
        
        -- Lock Tâm (Aimbot)
        if Config.Aimbot then
            local closest, shortDist = nil, 200
            local center = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
            
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("Head") and plr.Character.Humanoid.Health > 0 then
                    local isEnemy = true
                    if Config.Faction == "Police" and plr.Team == LocalPlayer.Team then isEnemy = false end
                    if isEnemy then
                        local pos, onScreen = Camera:WorldToViewportPoint(plr.Character.Head.Position)
                        if onScreen then
                            local dist = (Vector2.new(pos.X, pos.Y) - center).Magnitude
                            if dist < shortDist then
                                shortDist = dist
                                closest = plr.Character.Head
                            end
                        end
                    end
                end
            end
            if closest then
                Camera.CFrame = CFrame.new(Camera.CFrame.Position, closest.Position)
            end
        end
    end)
end)

-- 5. GIAO DIỆN MENU V4 PRO (CHUẨN HOẠT ĐỘNG 100%)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ZakaV4Ultimate"
ScreenGui.ResetOnSpawn = false
pcall(function() ScreenGui.Parent = game:GetService("CoreGui") end)
if not ScreenGui.Parent then ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end

local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size = UDim2.new(0, 55, 0, 55)
ToggleBtn.Position = UDim2.new(0, 10, 0.25, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
ToggleBtn.Text = "ZAKA\nV4"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 12
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(1, 0)

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 310, 0, 350)
Main.Position = UDim2.new(0.5, -155, 0.5, -175)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 22)
Main.Active = true
Main.Draggable = true
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10)

ToggleBtn.MouseButton1Click:Connect(function() Main.Visible = not Main.Visible end)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(22, 22, 32)
Title.Text = "   ZAKA V4 ULTIMATE - REBUILT"
Title.TextColor3 = Color3.fromRGB(0, 255, 150)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 13
Title.TextXAlignment = Enum.TextXAlignment.Left
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 10)

local Scroll = Instance.new("ScrollingFrame", Main)
Scroll.Size = UDim2.new(1, -20, 1, -50)
Scroll.Position = UDim2.new(0, 10, 0, 45)
Scroll.BackgroundTransparency = 1
Scroll.CanvasSize = UDim2.new(0, 0, 0, 380)

local Layout = Instance.new("UIListLayout", Scroll)
Layout.Padding = UDim.new(0, 6)

local function AddToggle(txt, callback)
    local f = Instance.new("Frame", Scroll)
    f.Size = UDim2.new(1, 0, 0, 38)
    f.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    Instance.new("UICorner", f).CornerRadius = UDim.new(0, 6)

    local l = Instance.new("TextLabel", f)
    l.Size = UDim2.new(1, -60, 1, 0)
    l.Position = UDim2.new(0, 10, 0, 0)
    l.BackgroundTransparency = 1
    l.Text = txt
    l.TextColor3 = Color3.fromRGB(240, 240, 240)
    l.Font = Enum.Font.Gotham
    l.TextSize = 11
    l.TextXAlignment = Enum.TextXAlignment.Left

    local b = Instance.new("TextButton", f)
    b.Size = UDim2.new(0, 40, 0, 20)
    b.Position = UDim2.new(1, -48, 0.5, -10)
    b.BackgroundColor3 = Color3.fromRGB(50, 50, 65)
    b.Text = ""
    Instance.new("UICorner", b).CornerRadius = UDim.new(1, 0)

    local state = false
    b.MouseButton1Click:Connect(function()
        state = not state
        b.BackgroundColor3 = state and Color3.fromRGB(0, 200, 100) or Color3.fromRGB(50, 50, 65)
        callback(state)
    end)
end

-- THÊM CÁC TÍNH NĂNG VÀO MENU
AddToggle("Chế độ Cảnh Sát (Bật: Cop / Tắt: Civ)", function(v) Config.Faction = v and "Police" or "Civilian" end)
AddToggle("[Police] Auto Bắt Tội Phạm (Smart)", function(v) Config.AutoArrest = v end)
AddToggle("[Civilian] Auto Farm Buôn Lậu", function(v) Config.AutoTrade = v end)
AddToggle("Lock Tâm Aimbot", function(v) Config.Aimbot = v end)
AddToggle("Tăng Tốc Di Chuyển (Speed Hack)", function(v) 
    Config.EnableSpeed = v 
    Config.WalkSpeed = v and 28 or 16
end)

print("Zaka V4 Ultimate Rebuilt Loaded Successfully!")
